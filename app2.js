function loginModal(){if(TOKEN)return'';const setup=S.loginSetup;return `<div class="modal loginmodal"><div class="modalin loginbox"><div class="label">European Swing</div><h2>${S.loginName?esc(S.loginName):'Who are you?'}</h2>${!S.loginName?`<div class="names">${['Ishaan','Nihal','Sarhan'].map(n=>`<button class="namebtn loginName" data-name="${n}">${n}</button>`).join('')}</div>`:S.loginBusy?`<div class="notice">Checking profile…</div><button class="ghost backbtn" id="backLogin">Back</button>`:`<div class="pinwrap"><input id="pinInput" class="pininput" type="password" inputmode="numeric" maxlength="4" pattern="[0-9]*" placeholder="${setup?'Set 4-digit PIN':'4-digit PIN'}" autocomplete="off" autofocus><button class="action" id="loginBtn">${setup?'Set PIN':'Enter'}</button><button class="ghost backbtn" id="backLogin">Back</button>${S.loginError?`<div class="loginerror">${esc(S.loginError)}</div>`:''}</div>`}</div></div>`}
function accountModal(){if(!S.accountOpen||!TOKEN)return'';return `<div class="modal"><div class="modalin loginbox"><button class="close" id="closeAccount">×</button><div class="label">${esc(S.user)}</div><h2>Account</h2><div class="pinwrap"><input id="currentPin" class="accountinput" type="password" inputmode="numeric" maxlength="4" placeholder="Current PIN"><input id="newPin" class="accountinput" type="password" inputmode="numeric" maxlength="4" placeholder="New 4-digit PIN"><button class="action" id="changePinBtn" ${S.accountBusy?'disabled':''}>${S.accountBusy?'Changing…':'Change PIN'}</button>${S.accountError?`<div class="loginerror">${esc(S.accountError)}</div>`:''}<button class="ghost backbtn" id="switchUserBtn">Switch user</button></div></div></div>`}
function filtered(){return S.filter?S.rank.filter(p=>p.points===S.filter):S.rank}
function playersHtml(){return filtered().map(p=>{const sel=S.roster.some(x=>x.name===p.name);const blocked=!sel&&(S.roster.length>=5||totalCost()+p.points>12);return `<button class="player ${sel?'sel':''} ${blocked?'disabled':''}" data-p="${esc(p.name)}" ${blocked?'disabled':''}><span>${esc(p.name)}</span><span class="rating r${p.points}">${p.points}</span></button>`}).join('')}
function rosterHtml(){let rows=[0,1,2,3,4].map(i=>{const p=S.roster[i];return p?`<div class="slot"><span>${esc(p.name)}</span><span><b class="num">${p.points}</b> <button class="remove" data-rm="${esc(p.name)}">×</button></span></div>`:`<div class="slot empty"><span>Pick ${i+1}</span><span>—</span></div>`}).join('');if(S.chip==='AND')rows+=S.extra?`<div class="slot and"><span>${esc(S.extra.name)} <span class="tag">AND</span></span><span><b class="num">${S.extra.points}</b> <button class="remove" id="removeExtra">×</button></span></div>`:`<div class="slot and empty"><span>6th pick · AND</span><span>—</span></div>`;return rows}
function ownLocked(){const st=S.status;if(!st?.submitted)return'';const all=[...(st.picks||[]),...(st.extra_player?[st.extra_player]:[])];return `<div class="notice locked"><b>Submitted ✓</b><br>Other teams stay hidden until Thursday 12:00 AM IST.</div><div class="roster" style="margin-top:12px">${all.map(n=>{const p=S.rank.find(x=>x.name===n);const tags=[st.extra_player===n?'AND':'',st.double_player===n?'2×':'',st.cut_player===n?'CUT':''].filter(Boolean).map(x=>` <span class="tag">${x}</span>`).join('');return `<div class="slot"><span>${esc(n)}${tags}</span><b class="num">${p?.points??''}</b></div>`}).join('')}</div>${!revealedNow(ev())?`<button class="ghost rulesbtn" id="editEntry">Edit picks</button>`:''}${st.chip?'':`<div class="cutbox"><label>CUT · after Round 1</label><div class="cutrow"><select id="cutSelect" class="select"><option value="">Select player</option>${(st.picks||[]).map(n=>`<option>${esc(n)}</option>`).join('')}</select><button class="ghost" id="cutBtn">Use CUT</button></div><small>Target: Friday 12:00 AM IST. Late use is still recorded.</small></div>`}`}
function revealHtml(){const rows=S.revealed||[];const counts={};rows.forEach(t=>[...(t.picks||[]),...(t.extra_player?[t.extra_player]:[])].forEach(n=>counts[n]=(counts[n]||0)+1));const by=Object.fromEntries(rows.map(x=>[(x.player_name||'').toLowerCase(),x]));return `<div class="reveal">${['ishaan','nihal','sarhan'].map(u=>{const t=by[u];if(!t)return `<div class="team"><h3>${u}</h3><div class="notice">No submission</div></div>`;const all=[...(t.picks||[]),...(t.extra_player?[t.extra_player]:[])];return `<div class="team"><h3>${u}</h3><ol>${all.map(n=>{const p=S.rank.find(x=>x.name===n);return `<li class="${counts[n]>1?'dup':'unique'}">${esc(n)} <b>${p?.points??''}</b>${t.extra_player===n?' <span class="tag">AND</span>':''}${t.double_player===n?' <span class="tag">2×</span>':''}${t.cut_player===n?' <span class="tag">CUT</span>':''}</li>`}).join('')}</ol></div>`}).join('')}</div>`}
function pickPanel(){const e=ev();if(S.loading)return `<div class="card pad"><div class="notice">Loading…</div></div>`;if(!TOKEN)return `<div class="card pad"><div class="notice">Choose your name to enter the league.</div></div>`;if(S.error)return `<div class="card pad"><h2>Link problem</h2><div class="notice">${esc(S.error)}</div></div>`;if(e.id!=='irish-open')return `<div class="card pad"><div class="titlebar"><h2>${e.name}</h2></div><div class="notice">Ranking list will appear here when the field is ready.</div></div>`;if(revealedNow(e))return `<div class="card pad"><div class="titlebar"><h2>Teams</h2><span class="label">Revealed</span></div>${revealHtml()}</div>`;if(S.status?.submitted&&!S.roster.length)return `<div class="card pad"><div class="titlebar"><h2>Your entry</h2><span class="label">${esc(S.user)}</span></div>${ownLocked()}</div>`;const remain=12-rosterCost();const andAvail=S.rank.filter(p=>!S.roster.some(r=>r.name===p.name)&&p.points<=remain);return `<div class="card pad"><div class="summary"><div class="stat"><span>Picks</span><strong>${S.roster.length+(S.chip==='AND'&&S.extra?1:0)} / ${S.chip==='AND'?6:5}</strong></div><div class="stat ok"><span>Budget</span><strong>${totalCost()} / 12</strong></div><div class="stat"><span>Chip</span><strong>${S.chip||'—'}</strong></div></div><div class="searchrow"><input class="search" id="search" placeholder="Search player"></div><div class="filters">${[0,5,4,3,2,1].map(x=>`<button class="filter ${S.filter===x?'active':''}" data-f="${x}">${x?x+' pt'+(x>1?'s':''):'All'}</button>`).join('')}</div><div class="players">${playersHtml()}</div></div><div class="card pad" style="margin-top:14px"><div class="titlebar"><h2>Your team</h2><span class="label">${12-totalCost()} pts left</span></div><div class="roster">${rosterHtml()}</div><div class="chiprow"><button class="chip ${S.chip==='AND'?'on':''}" data-chip="AND"><b>AND</b><span>6th pick · 2× season</span></button><button class="chip ${S.chip==='DOUBLE'?'on':''}" data-chip="DOUBLE"><b>DOUBLE</b><span>2× one player</span></button><button class="chip" disabled><b>CUT</b><span>After R1</span></button></div>${S.chip==='AND'?`<div class="andpick"><select id="extra" class="select"><option value="">6th player · max ${remain} pts</option>${andAvail.map(p=>`<option value="${esc(p.name)}" ${S.extra?.name===p.name?'selected':''}>${esc(p.name)} · ${p.points}</option>`).join('')}</select></div>`:''}${S.chip==='DOUBLE'?`<div class="doublepick"><select id="double" class="select"><option value="">Player to double</option>${S.roster.map(p=>`<option ${S.double===p.name?'selected':''}>${esc(p.name)}</option>`).join('')}</select></div>`:''}<button class="action" id="submit" ${S.roster.length!==5||(S.chip==='AND'&&!S.extra)?'disabled':''}>Submit picks</button></div>`}
function side(){return `<div class="card pad"><div class="titlebar"><h3>Standings</h3><span class="label">Overall</span></div><div class="standings">${['Ishaan','Nihal','Sarhan'].map((u,i)=>`<div class="standing"><span class="pos">${i+1}</span><span class="name">${u}</span><span class="pts">0</span></div>`).join('')}</div></div><div class="card pad" style="margin-top:14px"><div class="titlebar"><h3>Events</h3></div><div class="events">${EVENTS.map(x=>`<div class="event ${S.event===x.id?'current':''}"><span class="rnd">${x.r}</span><div><b>${x.name}</b><small>${x.m!==1?x.m+'× points':x.chips?'Chips available':'No chips'}</small></div><button class="ghost ev" data-e="${x.id}">View</button></div>`).join('')}</div><button class="ghost rulesbtn" id="rules">Rules & scoring</button></div>`}
function rulesModal(){if(!S.showRules)return'';return `<div class="modal"><div class="modalin"><button class="close" id="closeRules">×</button><div class="label">European Swing</div><h2>Rules</h2><div class="rulelist"><div class="ruleline"><b>Weekly team</b><span>5 players · 12 points</span></div><div class="ruleline"><b>Picks target</b><span>Wednesday · 10pm IST</span></div><div class="ruleline"><b>Reveal</b><span>Thursday · 12am IST</span></div><div class="ruleline"><b>AND</b><span>6th player within 12 points · 2× season</span></div><div class="ruleline"><b>DOUBLE</b><span>Double one player's score · 1× season</span></div><div class="ruleline"><b>CUT</b><span>After R1 · Friday 12am IST target · 1× season</span></div><div class="ruleline"><b>Chip limit</b><span>1 per event · none in Abu Dhabi/Dubai</span></div><div class="ruleline"><b>Abu Dhabi</b><span>1.5× points</span></div><div class="ruleline"><b>Dubai</b><span>2× points</span></div></div><h3 style="margin:18px 0 8px">Scoring</h3><div class="scoregrid">${SC.map(s=>`<div class="score"><span>${s[0]}</span><b>${s[1]>0?'+':''}${s[1]}</b></div>`).join('')}</div></div></div>`}
